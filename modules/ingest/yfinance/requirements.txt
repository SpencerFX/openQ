# yfinance per-exchange OHLCV ingest (universe / backfill / to_kdb / feed)
yfinance
pandas>=2.0
pyarrow>=14
openpyxl>=3.1          # hkex universe (.xlsx)
xlrd>=2.0.1            # nikkei universe (JPX data_j.xls, legacy .xls)

# only for `feed.py --sink tp` (publish to an openQ tickerplant)
qpython
numpy
